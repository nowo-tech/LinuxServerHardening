---
# Non-secret defaults. Copy to vars.yml and adjust.
# Prefer safer production defaults; see comments for lab overrides.
# Always pass -e @profiles/lab.yml or -e @profiles/prod.yml on harden (sets harden_profile).

harden_admin_user: admin
harden_ssh_port: 2222
harden_ssh_public_key_path: "~/.ssh/lab_ed25519.pub"
harden_ssh_mfa_enable: false
harden_ssh_mfa_nullok: true
harden_ssh_max_auth_tries: 3
harden_ssh_max_sessions: 3
harden_ssh_max_startups: "10:30:60"
# Avoid PTR-dependent auth delays on typical VPS networks
harden_ssh_use_dns: "no"
# Exceptional recovery only — harden play refuses root inventory by default
harden_allow_root_harden: false

harden_groups:
  ssh: sshaccess
  sudo: elevated
  su: switchroot

# Lab convenience: set true so Ansible can become root without a password prompt
harden_passwordless_sudo: false
# Lab convenience: set true only on disposable VMs
harden_auto_reboot: false
harden_auto_reboot_time: "04:30"

harden_mail_to: security@example.com
harden_mail_from: server-alerts@example.com
harden_smtp_host: smtp.example.com
harden_smtp_port: 587
# Set false only for local sinks / lab relays without STARTTLS
harden_smtp_tls: true
harden_smtp_auth: true
# CI/lab: skip the live mail(1) probe after rendering msmtp
harden_skip_mail_test: false

harden_ntp_pool: pool.ntp.org
harden_ntp_servers:
  - pool.ntp.org

harden_ufw_allow_out_ports:
  - { port: "53", proto: "any", comment: "DNS" }
  - { port: "123", proto: "udp", comment: "NTP" }
  - { port: "43", proto: "tcp", comment: "WHOIS (Fail2Ban action_mwl)" }
  - { port: "80", proto: "tcp", comment: "HTTP" }
  - { port: "443", proto: "tcp", comment: "HTTPS" }
  - { port: "587", proto: "tcp", comment: "SMTP submission" }

harden_ufw_allow_in: []

harden_fail2ban_bantime: 1h
harden_fail2ban_maxretry: 5
# Put your static admin/VPN CIDRs here (required unless using profiles/lab.yml)
harden_fail2ban_ignoreip: ""
# Default true: refuse empty/documentation ignoreip. Lab profile sets false.
harden_fail2ban_require_ignoreip: true
# When true, mail/psad/lynis soft-fails become hard failures
harden_strict_ops: true

# When true, authorized_keys is replaced by the single configured key (rotation)
harden_ssh_keys_exclusive: false

harden_enable_psad: true
# Auto-block can lock out admins/CI — opt in deliberately
harden_psad_auto_ids: false
harden_psad_danger_level: 3

harden_enable_clamav: false
harden_enable_rkhunter: true
harden_enable_chkrootkit: false
harden_enable_aide: false
harden_enable_logwatch: true
harden_enable_mfa_role: false
harden_enable_lynis_mail_report: true

# Monitoring stack (role always runs — false stops/uninstalls idempotently)
harden_enable_node_exporter: false
harden_node_exporter_listen: "127.0.0.1:9100"
# Remote scrape is opt-in; prefer SSH tunnel / VPN
harden_node_exporter_ufw_allow: false
harden_node_exporter_allow_from:
  - any

harden_enable_health_watchdog: false
harden_health_disk_threshold_pct: 90
harden_health_watchdog_cron: "*/5 * * * *"
harden_health_watchdog_mail: true
harden_health_probe_urls: []
harden_health_probe_timeout: 10

harden_enable_monit: false
harden_monit_daemon_seconds: 120
harden_monit_load_1min: 8
harden_monit_memory_pct: 90
harden_monit_cpu_pct: 95

# Deploy agents (role always runs — false stops/uninstalls idempotently)
harden_enable_gha_runner: false
harden_gha_runner_version: "2.337.0"
harden_gha_runner_arch: x64
# Optional: harden_gha_runner_checksum: "sha256:..."
harden_gha_runner_url: "REPLACE_ME_https://github.com/ORG/REPO"
harden_gha_runner_name: ""
harden_gha_runner_labels: "self-hosted,linux,debian"
harden_gha_runner_user: gha-runner
harden_gha_runner_home: /opt/actions-runner
harden_gha_runner_purge_home: true
harden_gha_runner_purge_user: false

harden_enable_webhook_deploy: false
harden_webhook_deploy_listen: "127.0.0.1:9000"
harden_webhook_deploy_path: /hooks/deploy
harden_webhook_deploy_repo_path: /var/www/app
harden_webhook_deploy_repo_url: ""
harden_webhook_deploy_branch: main
harden_webhook_deploy_restart_service: ""
harden_webhook_deploy_ufw_allow: false
harden_webhook_deploy_allow_from:
  - any
# Optional deploy key path on the host for private repos
# harden_webhook_deploy_ssh_key: /root/.ssh/deploy_ed25519
# Lynis on every harden run is slow; 03-audit.yml forces a run
harden_lynis_force_run: false
# Third-party Lynis APT repo is OFF by default (supply-chain gate)
harden_allow_third_party_lynis_repo: false
# Optional: pin key fetch when opting into third-party repo
# harden_cisofy_key_checksum: "sha256:<paste-current-key-hash>"

harden_clamav_scan_paths:
  - /home
  - /var/www
  - /tmp
  - /opt

harden_packages:
  - apt-listchanges
  - apt-transport-https
  - apticron
  - audispd-plugins
  - auditd
  - ca-certificates
  - curl
  - fail2ban
  - libpam-pwquality
  - mailutils
  - msmtp
  - msmtp-mta
  - psad
  - rkhunter
  - ufw
  - unattended-upgrades
