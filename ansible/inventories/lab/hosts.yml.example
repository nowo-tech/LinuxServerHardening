# Runtime inventory — after bootstrap, connect as the admin user
all:
  children:
    hardened_servers:
      hosts:
        lab-debian:
          ansible_host: 203.0.113.10
          ansible_user: admin
          ansible_port: 2222
          ansible_python_interpreter: /usr/bin/python3
          # ansible_ssh_private_key_file: ~/.ssh/lab_ed25519
